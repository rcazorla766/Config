apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-house-predictor
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: api
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: "1"
              memory: 768Mi
