apiVersion: v1
kind: Service
metadata:
  name: ai-house-predictor
  labels:
    app.kubernetes.io/name: ai-house-predictor
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: mlops-tfm
  annotations:
    kubernetes.io/description: "ClusterIP service for the inference API"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-house-predictor
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
